# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 소프트웨어 개발 정보

이 소프트웨어 개발은 다음의 항목을 이용해서 개발한다. 

- Python 3.14를 이용해서 개발한다.
- 코드 테스트는 unittest를 활용한다. 
- 순환복잡도, 함수 라인수 등 측정 지표는 오픈소스 도구를 이용한다. 

## 프로젝트 개발 정책

### 개발 생명 주기

- 이 프로젝트는 **반드시** 분석, 설계, 구현, 테스트의 순서로 개발을 진행한다.
- 각 단계가 완료됐을 때, 지정된 템플릿을 이용한 산출물이 생성돼야 한다.

### 분석 지침

- 요구사항 분석 단계 수행은 requirement-analyst 서브에이전트가 담당한다.

### 아키텍처 설계 지침

- 아키텍처 설계 단계 수행은 architecture-designer 서브에이전트가 담당한다.

### 구현 지침

- 구현 단계 수행은 coding 서브에이전트가 담당한다.
- TDD 방식으로 진행하고 TDD 스킬을 사용해야 한다.
- 다음의 품질 지표를 **반드시** 준수해야한다 
  - 함수 라인수는 순수코드라인 50라인 이하여야 한다.
  - 함수 순환복잡도는 10 이하여야 한다.
  - 중복 코드는 7라인까지 허용한다.
  - 주석은 Doxygen 방식으로 작성하며, 20%이상 작성해야 한다.
- 함수명, 변수명은 3글자 이상 사용하고, 낙타 표기법을 사용한다. 

### 단위 테스트 지침

- 단위 테스트(SWE.4)는 `coding` 서브에이전트가 `tdd`+`code-quality-gates` 스킬로 수행한다. 단위 테스트는 TDD로 대체한다.
- 테스트 베이시스는 상세설계의 함수 계약·경계값·의사결정표이며, 검증 대상은 함수/모듈 내부 로직(화이트박스)이다.
- 단위 테스트는 Branch 커버리지 100%를 달성해야 한다.

### 통합 테스트 지침

- 통합 테스트(SWE.5)는 `integration-tester` 서브에이전트가 `integration-testing` 스킬로 수행한다.
- 테스트 베이시스는 아키텍처 설계서(architecture-design 스킬 산출물)의 인터페이스 계약과 통합 순서이며, 검증 대상은 컴포넌트 간 인터페이스 상호작용(그레이박스)이다.
- 테스트 기법은 ISO 26262 Part 6 Clause 10에 근거하고, 함수 커버리지·콜(Call) 커버리지 100%를 달성해야 한다.

### 시스템 테스트 지침

- 시스템(자격) 테스트(SWE.6)는 `sw-system-tester` 서브에이전트가 `sw-system-test` 스킬로 수행한다.
- 테스트 베이시스는 SW 요구사항 명세서 하나뿐이며, 검증 대상은 통합된 소프트웨어 전체의 대외 동작(블랙박스)이다. 아키텍처 내부 구조나 코드를 근거로 케이스를 만들지 않는다.

### 테스트 수준 간 경계

- 세 테스트 수준(단위/통합/시스템)은 서로 다른 테스트 베이시스와 검증 대상을 가지며, 어느 한 수준의 서브에이전트/스킬이 다른 수준의 검증을 대신 수행하지 않는다. 한 수준에서 다른 수준에 속하는 문제를 발견하면 그 자리에서 대신 고치지 말고, 해당 수준의 서브에이전트에게 넘겨야 한다고 사용자에게 알린다.
- 테스트 성공률은 100%여야 한다.